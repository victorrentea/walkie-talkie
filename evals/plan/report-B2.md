# Test plan run — 2026-09-26 13:19

Verdicts: **PASS** = the app does what the plan expects · **BUG** = the plan's prediction of a defect was confirmed · **FAIL** = neither the expectation nor the prediction · **SKIP** / **ERROR**.

BUG 5 · FAIL 1 · PASS 4

| case | verdict | s | expectation | observed |
|---|---|---|---|---|
| TG1 | **BUG** | 2 | 🔼 F7 at idle opens a caret prompt (pasteMode, chip `at caret`). Predicted defect: the key trace is blind to it (no `↓ key 98` line — the F-key branches return nil, not swallow()) | caret prompt opened; the trace is blind to the swallowed F7 — listening=True 126 ms after the POST, pasteMode=True, chip «Prompting → ￼... \| at caret \| ×1», trace ↓98×0, ↑98 (ours) passed×1 · sent forward-click@0.682s |
| TG2 | **PASS** | 5 | F10 train at 0/300/600/900 ms → one start and three `re-triggered … dropped`; at 1.6 s `only NNN ms old — not stopping`; at 2.3 s it stops. Predicted defect (R21): the 2 s dwell runs from the main-thread edge, so the 2.3 s stop is refused too | starts=1, re-triggered at ['304', '302', '292'] ms, refused at sentence ages ['1583'] ms, stopped by +3.2 s=True · sent forward-right@0.000s forward-right@0.304s forward-right@0.605s forward-right@0.901s forward-right@1.605s forward-right@2.301s |
| TG6 | **PASS** | 5 | 🔽 then 🔽→ on ElevenLabs stops the clean sentence at both gaps. Predicted defect (R21): at +60 ms the tap has not heard of the sentence yet — `🔽 → — Return`, keycode 36 in the sink, the sentence still open; at +500 ms the correct stop | +61 ms: stray Return=False, stop=True, sink Returns=0, open 1 s on=False · +501 ms: stray Return=False, stop=True, sink Returns=0, open 1 s on=False · sent back-click@0.001s back-right@0.061s back-click@2.215s back-right@2.716s |
| TG7 | **PASS** | 64 | the same 🔽/🔽→ pair on Engine = wispr stops Wispr's sentence at +60 ms and at +500 ms (the arm is set on the tap thread — the asymmetry with T-G6) | +62 ms: stop branch=True, sink [] · +505 ms: stop branch=True, sink [] · Wispr closed after each: True/True · sent back-click@0.001s back-right@0.063s back-click@31.612s back-right@32.117s |
| TG14 | **BUG** | 5 | a kamikaze flick that re-fires (+2.5 s / +2.8 s) toggles once. Predicted defect: F9 has no re-fire guard — on, then `taken back` | on×1, taken back×1, chip row=False · sent forward-right@0.000s forward-down@2.503s forward-down@2.805s |
| TG15 | **BUG** | 5 | a film flick that re-fires (+2.5 s / +2.8 s) starts one recording. Predicted defect: F4 has no re-fire guard — start then stop (`caught no frames` / a few frames) | film started×1, stopped=🎬 recording stopped — 2 frames over 0.3s, filming=False, chip «Prompting → ￼... \| bind to send \| 0.3s (2￼) \| 🎥 0.3s, 2 frames» · sent forward-right@0.000s back-up@2.504s back-up@2.805s |
| TG17 | **BUG** | 3 | 🔼↑ F8 on a caret sentence is refused visibly (a log line / flash), not converted. Predicted defect: silent — `convertDictationToSpawn` returns false with no word | spawnPending=False, pasteMode=True, log [], new chip rows [] · sent forward-click@0.683s forward-up@1.085s |
| TG25 | **BUG** | 11 | main stalled 8 s: an F10 at +1.0 s is queued (acts after the stall); at +4.5 s the tap is failing open. Expected: a ⌃⌥⌘F-key never reaches the front app. Predicted defect: it leaks — the sink records ⌃⌥⌘F10 (`🧊 main thread silent`, trace `failing open`) | stall line=True, 🧊 opened=True, trace ↓F10 failing open=True, sink F-keys ["keyDown:'\\uf70d'"], the +1.0 s F10 acted after the stall=True · sent forward-right@1.002s forward-right@4.505s |
| TG26 | **FAIL** | 11 | the canary during fail-open says the tap is alive (it is — it is failing open on purpose). Predicted defect: `alive:false` (misdiagnosis), and its V keyUp passes to the front app | the fail-open never opened — POST /test/firewall at +4.5 s → 200 alive=False canaryMs=500; 🧊 opened=False; `did NOT see` line=True; trace ↑V failing open=True (the sink records keyDowns only, so the keyUp is read off the trace) |
| TG30 | **PASS** | 1 | a `/test/gesture` 200 is backed by the tap's own log line (the installed build posts for real). Predicted defect on a debug build: 200 with no effect — so every assertion reads the log, never the 200 | HTTP 200 ⌃⌥⌘F9; effect line=True; tap saw the keyUp=True; relay binary /Applications/Walkie Talkie.app/Contents/MacOS/Walkie Talkie · sent forward-down@0.640s |
