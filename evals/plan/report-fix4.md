# Test plan run — 2026-09-26 17:19

Verdicts: **PASS** = the app does what the plan expects · **BUG** = the plan's prediction of a defect was confirmed · **FAIL** = neither the expectation nor the prediction · **SKIP** / **ERROR**.

PASS 14

| case | verdict | s | expectation | observed |
|---|---|---|---|---|
| TL21 | **PASS** | 10 | start+stop both queued inside a 6 s main stall → neither acted on (the recorder never opens), a `⏸ a start and its stop were both queued` line and the chip's `⏸ ignored — the Mac was frozen` (batch 4 decision, 2026-09-26). Before: a silent sub-0.35 s discard | mic opened=False; coalesced line=True; 'discarded — under 0.35s'=False; dwell refusal=False; after the stall listening=False isRecording=False; banner rows=['⏸ ignored — the Mac was frozen'] — neither toggle acted on, and the chip said why |
| TR4 | **PASS** | 9 | fail-open proves itself (/test/stall 6 + input) → 🧊 within 3.5 s, one hangs/ file, no button left down | nudge posted=True; silent line=3.1 s; back line=6.1 s; new hangs files=1; sessionFlags=[]; buttons down=[] |
| TG1 | **PASS** | 2 | 🔼 F7 at idle opens a caret prompt (pasteMode, chip `at caret`). Predicted defect: the key trace is blind to it (no `↓ key 98` line — the F-key branches return nil, not swallow()) | listening=True 384 ms after the POST, pasteMode=True, chip «Prompting → ￼... \| at caret \| ×1», trace ↓98×1, ↑98 (ours) passed×1 · sent forward-click@0.685s |
| TG2 | **PASS** | 4 | F10 train at 0/300/600/900 ms → one start and three `re-triggered … dropped`; at 1.6 s `only NNN ms old — not stopping`; at 2.3 s it stops. Predicted defect (R21): the 2 s dwell runs from the main-thread edge, so the 2.3 s stop is refused too | starts=1, re-triggered at ['309', '297', '300'] ms, refused at sentence ages ['1570'] ms, stopped by +3.2 s=True · sent forward-right@0.000s forward-right@0.305s forward-right@0.603s forward-right@0.904s forward-right@1.602s forward-right@2.305s |
| TG10 | **PASS** | 5 | 🔼← F11 while the prompt panel is held cancels it (`✕ cancelled`, no outbox row). Predicted defect: nothing — the panel sends at the end of its hold | panel up 29 ms after /test/dictation, F11 at +0.029 s; outbox +0, ✕ cancelled=True, autosend=True · sent forward-left@0.029s |
| TG11 | **PASS** | 4 | 🔽→ while the panel is held is not the panel's ⏎ (its Return is stamped ours). Predicted defect: the ⏎ branch ignores the stamp — trace `SWALLOWED by the prompt panel's ⏎` on key 36 (ours), panel sent | panel up 25 ms after /test/dictation; outbox row 1628 ms after the 🔽→; trace: swallowed by the panel=False, passed=True; sink Returns=0; autosend=True · sent back-right@0.025s |
| TG14 | **PASS** | 5 | a kamikaze flick that re-fires (+2.5 s / +2.8 s) toggles once. Predicted defect: F9 has no re-fire guard — on, then `taken back` | on×1, taken back×0, chip row=True · sent forward-right@0.000s forward-down@2.504s forward-down@2.804s |
| TG15 | **PASS** | 5 | a film flick that re-fires (+2.5 s / +2.8 s) starts one recording. Predicted defect: F4 has no re-fire guard — start then stop (`caught no frames` / a few frames) | film started×1, stopped=None, filming=True, chip «Prompting → ￼... \| bind to send \| 0.0s (0￼)» · sent forward-right@0.000s back-up@2.505s back-up@2.802s |
| TG16 | **PASS** | 4 | 🔼↓ F9 while the panel is held (words not yet committed) marks that prompt kamikaze. Predicted defect: `☠️ kamikaze gesture with no sentence in flight — ignored` | panel up 20 ms; ignored line=False; sent prompt carries kamikaze=True · sent forward-down@0.020s |
| TG17 | **PASS** | 3 | 🔼↑ F8 on a caret sentence stays ignored (Q7: no conversion, no flash) and says so in one log line. Predicted defect: silent — `convertDictationToSpawn` returns false with no word | spawnPending=False, pasteMode=True, log ['09-26 17:20:09 [relay] ✨ 🔼 ↑ on a caret sentence — ignored (Q7: it stays at the caret)'], new chip rows [] · sent forward-click@0.677s forward-up@0.814s |
| TG25 | **PASS** | 12 | main stalled 8 s: an F10 at +1.0 s is queued (acts after the stall); at +4.5 s the tap is failing open. Expected: a ⌃⌥⌘F-key never reaches the front app. Predicted defect: it leaks — the sink records ⌃⌥⌘F10 (`🧊 main thread silent`, trace `failing open`) | stall line=True, 🧊 opened=True, trace ↓F10 failing open=False, sink F-keys [], the +4.5 s F10 dropped while frozen=True, the +1.0 s F10 acted after the stall=True · sent forward-right@1.006s forward-right@4.507s |
| TG26 | **PASS** | 11 | the canary during fail-open says the tap is alive (it is — it is failing open on purpose). Predicted defect: `alive:false` (misdiagnosis), and its V keyUp passes to the front app | POST /test/firewall at +4.5 s → 200 alive=True tap=open canaryMs=4.941999999573454; 🧊 opened=True; `did NOT see` line=False; trace ↑V failing open=False (the sink records keyDowns only, so the keyUp is read off the trace) |
| TG28 | **PASS** | 8 | every gesture's keyDown is in the key trace with its verdict (`↓ key N … SWALLOWED by …`). Predicted defect: blindness for all ten — zero `↓`, one `↑ (ours) passed` each | forward-click F7 ↓1/1sw ↑1, forward-right F10 ↓1/1sw ↑1, forward-left F11 ↓1/1sw ↑1, forward-up F8 ↓1/1sw ↑1, forward-down F9 ↓1/1sw ↑1, back-click F6 ↓1/1sw ↑1, back-down F12 ↓1/1sw ↑1, back-right F5 ↓1/1sw ↑1, back-left F3 ↓1/1sw ↑1, back-up F4 ↓1/1sw ↑1 · sent back-right@0.000s forward-left@0.722s back-down@1.438s forward-down@2.164s forward-click@2.884s forward-right@3.026s forward-up@3.548s back-up@4.376s back-click@5.190s back-left@6.008s |
| TG29 | **PASS** | 14 | `sessionFlags == []` within 300 ms after every gesture and after the app's own posters (`postReturn` via 🔽→ at idle; `postWisprHandsFree` / `postWisprCancel` when Wispr runs) | 13 steps, every one bare within 300 ms · postWisprHandsFree exercised; postWisprCancel not reached (🔽← took the relay-cancel branch); Wispr closed=True · sent back-right@0.000s forward-left@0.738s back-down@1.458s forward-down@2.172s forward-click@2.901s forward-right@3.305s forward-up@3.825s back-up@4.646s back-click@5.463s back-left@6.285s postWisprHandsFree (start)@6.761s back-left@7.449s postWisprHandsFree (stop)@12.552s |
