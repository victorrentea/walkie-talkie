# Test plan run — 2026-09-26 12:40

Verdicts: **PASS** = the app does what the plan expects · **BUG** = the plan's prediction of a defect was confirmed · **FAIL** = neither the expectation nor the prediction · **SKIP** / **ERROR**.

BUG 12 · FAIL 7 · PASS 16 · SKIP 5

| case | verdict | s | expectation | observed |
|---|---|---|---|---|
| LC1 | **PASS** | 2 | first word centred: \|centre − bandWidth/2\| < 3, velocity 0, opacity 0 → 0.4 within 0.5 s, never anchor ≥ bandWidth − 5 | centre off 0.00 pt, max velocity 0.0, opacity 0 → [0.35] at +0.5 s → 0.4, bandWidth 1728 |
| LC2 | **FAIL** | 21 | growth at 0.4 s/word for 20 s: centre ±80 until line > bandWidth − 96, then anchor + shownWidth ≤ bandWidth − 48 + 2; \|Δanchor\| ≤ 700·Δt + 2; anchor rises only by a drop | centre off by 110 while narrow; 190/277 samples past the right margin (max 281 pt) — 277 samples, wide from t=6.501, max centre offset 110.2 pt while narrow, dropped 32, max velocity 575 |
| LC3 | **PASS** | 6 | pause: velocity → 0 within 1.5 s of the last word, centre unchanged until the eraser starts at 2.0 s | eraser at +2.04 s, max velocity 4.3 pt/s and centre drift 1.14 pt in [+1.5 s, eraser), velocity exactly 0 from +2.44 s |
| LC4 | **PASS** | 5 | burst of 15 words: anchor eases, no step > 700·Δt, every sample velocity ≤ 700 | max velocity 700 pt/s, 30 samples, dropped 1 |
| LC5 | **PASS** | 5 | "fix the build today" → "fix it today": corrections +1, ghosts [the, build] within 0.3 s, [] after 0.6 s, correcting [] after 2.7 s, reflowing > 0 then < 0.5 within 1.6 s, centre glides | corrections +1, ghosts [['the', 'build']], reflowing 129.8 → [0] at +1.6 s, update seen 0.01 s after the POST |
| LC6 | **PASS** | 3 | "500" → "five hundred": corrections +2, ghosts ["500"] | corrections +2, ghosts [['500']] |
| LC7 | **FAIL** | 1 | revision past the dropped words: dropped 0, words 3, centred at once (velocity 0 on the first sample), ghosts [] | centre off by -802 pt on the first sample — dropped before 13; first sample: dropped 0, anchor -69, velocity 0, centre off -802, corrections 3 |
| LC7b | **PASS** | 2 | change only inside the dropped region: corrections unchanged, anchor continuous | dropped 13 → 13, corrections +0, anchor -69.5 → -69.5 |
| LC8 | **PASS** | 4 | tail punctuation flicker and mid-line case/punctuation commits: corrections unchanged, ghosts [] | 6 revisions, corrections +0, 0 sample(s) with ghosts |
| LC9 | **FAIL** | 12 | append-only ×30: corrections 0; each appended word's opacity starts < 0.1 and reaches its target within 0.6 s | 5 word(s) slower than 0.65 s to 90 %: [(2, 0.68), (5, 0.66), (13, 0.66)] — corrections 0, start opacity max 0.09, slowest to 90 % 1.39 s over 30/30 words |
| LC10 | **PASS** | 3 | {on:false}: open flips at once, words 0 after 0.6 s; reopen within 0.15 s is not reset by the fade's completion | after close: open False words 0; after reopen at 0.1 s: open True words 2 (the panel's alpha is not in describe(): a fade that ends at 0 over a reopened band is invisible here) |
| LC11 | **SKIP** | 0 | second display: the band on the screen under the pointer (needs G7 frame) | needs G7 liveCaption.frame/screen |
| LC12 | **SKIP** | 0 | RELAY_SHOOT never shows the band | needs the app relaunched under RELAY_SHOOT (out of scope: no restarts from the runner) |
| LC14 | **PASS** | 1 | empty text while open: words 0, still open | words 0, open True |
| LC15 | **PASS** | 3 | {text:"a b c.", partial:"d e f"}: committed 3, opacity ≈ [1,1,1,0.8,0.6,0.4] within 0.6 s; commit all → ≈ 1 within 0.8 s, corrections 0 | +0.6 s [0.93, 0.93, 0.93, 0.74, 0.56, 0.37] (max err 0.07), settled [0.99, 0.99, 0.99, 0.79, 0.6, 0.4]; after commit +0.8 s [1, 1, 1, 0.99, 0.98, 0.97], corrections 0 |
| LC16 | **FAIL** | 11 | eraser after 2 s at ≈260 pt/s; visible centre ±80 while dropped grows; a new word freezes the front; after a full wipe the next word is a fresh centred line with eraseFront null | fresh line moving at 104 — eraser at +2.05 s, 263 pt/s; dropped 2, visible-centre offset max 53 pt over 14 samples; fresh line: eraseFront None, centre off 0.0, velocity 104 |
| LC17 | **PASS** | 4 | gentle correction: corrections +1, the eraser keeps sweeping (eraseFront still increasing) | corrections +1, front -757.2949019324733 → -465.8770977606764 (258 pt/s); the paler tint is not in describe() |
| LC18 | **SKIP** | 0 | timing precision needs G8's server-side script/trace, two displays G7's frame | needs G8 (/test/live-caption script + trace) |
| TL2 | **FAIL** | 3 | cancel of a test-open sentence → within 200 ms listening/settling false, 'nothing to cancel' line | cleared in 2027 ms; 'nothing to cancel' line: yes — still listening/settling after 2 s |
| TL3 | **FAIL** | 3 | POST /engine refused mid-sentence; accepted after cancel (whisper.loading:true when cold) | eleven-live→whisper: mid-sentence answer engine=eleven-live, after cancel engine=eleven-live; whisper loading=False ready=False |
| TL4 | **PASS** | 5 | /test/local-fallback happy path: via:local-fallback, ~6 s cold / ~2 s warm | first (cold): 4.42 s ok=True via=local-fallback text='If I dictate now, how good is this dictation, I wonder.'; second (warm): 0.7 s ok=True |
| TL7 | **BUG** | 3 | a dead helper must not read ready:true in /engine (plan: today still ready:true until a request fails) | after SIGKILL (alive=False): ready=True; after one request (0.0 s, ok=False): ready=False — ready stays true on a dead helper until a request trips over it |
| TL23 | **SKIP** | 0 | broken Python (RELAY_WHISPER_PYTHON=/nonexistent): ≤ 1 'did not come up' line per 10 s (~20 today) | needs an env change + relaunch of the app (RELAY_WHISPER_PYTHON=/nonexistent); out of scope for HTTP |
| TL27 | **PASS** | 4 | held sentence released by POST /bind {"tty"} within 1 s | released 0.37 s after the bind; words in the witness: yes; lastDelivery.to=terminal:ttys000; invariants ok |
| TL33 | **SKIP** | 0 | key hot-add under eleven-live reloads the live source too; today: 'key went away mid-sentence' + local-fallback | the key hot-add runs when the menu opens (status.elevenReady); needs Codex + moving elevenlabs.env |
| TR17 | **PASS** | 39 | restart gate: blocked while a sentence is open; opens no sooner than 10 s after the delivery (dry-run only — the SIGTERM / kill -9 halves are not run from the harness) | open sentence: exit 3 after 20.3 s (⛔️ still waiting for the sentence in flight: dictating after 20 s — gave up (--max-wait 20); nothing was restarted); after delivery: exit 0, gate opened 12.9 s after lastDelivery.at; invariants ok |
| TR21 | **BUG** | 3 | agent exited to the shell → '⛔️ refused', no delivery row in the outbox, still pastable (plan §3.11: today the row is written at commit) | guarded=True; refused=True; typed into zsh=False; outbox rows +1; lastDelivery.to=terminal:ttys003 — a refused sentence left a delivery receipt |
| TR28 | **PASS** | 0 | TCC identity: the launch line says bundle=ro.victorrentea.wispr-relay trusted=true eventTap=true (the concurrent build-app.sh half is not run from the harness) | last launch line: '09-26 11:20:19 [relay] accessibility trusted=true eventTap=true bundle=ro.victorrentea.wispr-relay'; pid 22498 started Sat Sep 26 11:20:18 2026; line from this launch=True; Info.plist id=ro.victorrentea.wispr-relay |
| TD1 | **BUG** | 2 | POST /bind {tty: ttys999} (no such tty) → 409; nothing bound, no sentence lost | dead tty bound, sentence lost with an outbox receipt — /bind 200 (bound tty=ttys999); lastDelivery.to=terminal:ttys999; outbox rows=['terminal:ttys999']; gone-line=True; bound after=None; awaitingBind=False |
| TD2 | **BUG** | 5 | two sentences held while unbound → the bind delivers both (or says the first was replaced) | first held sentence silently replaced — to1=held to2=held held-after-two=True; hold lines=2; after bind: ALFA in witness=False, BRAVO in witness=True; replacement said in log=False |
| TD4 | **BUG** | 5 | bind B during the hold of a sentence bound to A → words land in A (recipient fixed when the words arrived) | recipient changed during the hold — A=ttys000 B=ttys003 bind B → 200 at +0.1s; commit +0.60s vs bind done; to=terminal:ttys003; in A=False, in B=True |
| TD5 | **BUG** | 3 | a restore (POST /bind {tty}) during a caret dictation leaves it at the caret (pasteMode stays true) | restore took the caret back — /bind 200; pasteMode after=False; 'caret dictation redirected' logged=True |
| TD6 | **BUG** | 2 | a restore (POST /bind {tty}) during a spawn dictation keeps the spawn (spawnPending stays true) | restore took the spawn back — /bind 200; spawnPending after=False; '✨ spawn dropped' logged=True |
| TD10 | **PASS** | 24 | relay-restart.sh --dry-run --max-wait 20 with a sentence held for a bind → exit 3, 'held for a bind' | exit 3 after 20s; output: 🔍 waiting until Walkie Talkie (pid 22498) is idle and quiet for 10 s…<br>⏳ waiting for the sentence in flight: held for a bind<br>⛔️ still waiting for the sentence in flight: held for a bind after 20 s — gave up (--max-wait 20); nothing was restarted |
| TD13 | **BUG** | 5 | restoring a tmux binding (POST /bind {client tty}) binds the pane that was bound, not the active one | restore picked the active pane — client ttys006; panes ['%1', '%2']; bind with %1 active → 200 %1; bound-tty='ttys006'; restore with %2 active → 200 %2 |
| TD14 | **BUG** | 3 | restoring a tty that is no Terminal.app tab (IDE-like pty) → /bind refuses (409); no sentence lost | bound a pty with no tab; sentence lost with a receipt — /bind ttys003 → 200 (ttys003); ps: Ss+  cat; to=terminal:ttys003; rows=['terminal:ttys003']; 'no Terminal.app tab' logged=True; bound after=None; awaitingBind=False |
| TD15 | **BUG** | 7 | a shell behind `script -q /dev/null zsh` is refused by the guard; `touch` does not run | the sentence was executed by the shell behind `script` — tab ttys003 bind 200; guard saw foreground=['script']; refused=False; to=terminal:ttys003; rows=1; touch ran=True |
| TD16 | **BUG** | 8 | a sentence starting with q sent into `less` does not reach the shell underneath; `touch` does not run | q quit less and zsh ran the rest — tab ttys003 bind 200; guard saw foreground=['less']; to=terminal:ttys003; touch ran=True |
| TD19 | **BUG** | 5 | a 40-char sentence containing 'press Enter to send' into a plain reader gets no third Return | a third Return went to a tab that never asked — tab ttys003 bind 200; to=terminal:ttys003; third Return logged=True; newlines in file=5 |
| TD20 | **FAIL** | 56 | 5000 chars with quotes/$(date)/backticks/^C/ESC[201~ arrive intact: 2-3 CR, control bytes stripped, no expansion | the sentence did not arrive whole — sent 4826 chars; to=None; arrived 0 bytes in 0 reads (); CR=0; ^C raw=False; ESC[201~ raw=False; literals intact=False; begin+end=False \|  |
