--- a/src/libprojectM/Audio/PCM.cpp	2026-09-21 19:47:08
+++ b/src/libprojectM/Audio/PCM.cpp	2026-09-21 19:44:07
@@ -1,5 +1,7 @@
 #include "PCM.hpp"
 #include <cstdlib>
+#include <cmath>
+#include <algorithm>
 
 namespace libprojectM {
 namespace Audio {
@@ -22,6 +24,7 @@
     {
         size_t const bufferOffset = (m_start + i) % AudioBufferSamples;
         m_inputBufferL[bufferOffset] = 128.0f * (static_cast<float>(samples[0 + i * channels]) - float(signalOffset)) / float(signalAmplitude);
+        m_bcHistory[m_bcStart] = m_inputBufferL[bufferOffset]; m_bcStart = (m_bcStart + 1) % m_bcHistory.size();   // walkie (PM_BC_AUDIO)
         if (channels > 1)
         {
             m_inputBufferR[bufferOffset] = 128.0f * (static_cast<float>(samples[1 + i * channels]) - float(signalOffset)) / float(signalAmplitude);
@@ -96,15 +99,30 @@
     std::vector<float> waveformSamples(AudioBufferSamples);
     std::vector<float> spectrumValues;
 
-    size_t oldI{0};
-    for (size_t i = 0; i < AudioBufferSamples; i++)
+    // walkie: PM_BC_AUDIO=1 analyses the sound the way butterchurn does — the newest
+    // 1024 samples (44.1 kHz expected), byte-quantised, no damping and no window,
+    // through a 1024-point FFT. The waveform handed to the presets is untouched.
+    static const bool bc = getenv("PM_BC_AUDIO") != nullptr;
+    if (bc)
     {
-        // Damp the input into the FFT a bit, to reduce high-frequency noise:
-        waveformSamples[i] = 0.5f * (waveformData[i] + waveformData[oldI]);
-        oldI = i;
+        waveformSamples.resize(m_bcHistory.size());
+        for (size_t i = 0; i < m_bcHistory.size(); i++)
+            waveformSamples[i] = std::round(std::max(-128.0f, std::min(127.0f, m_bcHistory[(m_bcStart + i) % m_bcHistory.size()])));
+        static MilkdropFFT bcFft{m_bcHistory.size(), SpectrumSamples, true, -1.0f};
+        bcFft.TimeToFrequencyDomain(waveformSamples, spectrumValues);
     }
+    else
+    {
+        size_t oldI{0};
+        for (size_t i = 0; i < AudioBufferSamples; i++)
+        {
+            // Damp the input into the FFT a bit, to reduce high-frequency noise:
+            waveformSamples[i] = 0.5f * (waveformData[i] + waveformData[oldI]);
+            oldI = i;
+        }
 
-    m_fft.TimeToFrequencyDomain(waveformSamples, spectrumValues);
+        m_fft.TimeToFrequencyDomain(waveformSamples, spectrumValues);
+    }
 
     // walkie: the spectrum's scale, matched to butterchurn's (whose FFT output feeds
     // spectrum waves at a different magnitude). PM_SPECTRUM_SCALE overrides; the
--- a/src/libprojectM/Audio/PCM.hpp	2026-07-14 15:13:51
+++ b/src/libprojectM/Audio/PCM.hpp	2026-09-21 19:42:48
@@ -93,6 +93,9 @@
     WaveformBuffer m_inputBufferL{0.f}; //!< Circular buffer for left-channel PCM data.
     WaveformBuffer m_inputBufferR{0.f}; //!< Circular buffer for right-channel PCM data.
     std::atomic<size_t> m_start{0};     //!< Circular buffer start index.
+    // walkie (PM_BC_AUDIO): the newest 1024 mono samples, butterchurn's analysis window.
+    std::array<float, 1024> m_bcHistory{0.f};
+    size_t m_bcStart{0};
 
     // Frame waveform data
     WaveformBuffer m_waveformL{0.f}; //!< Left-channel waveform data, aligned. Only the first WaveformSamples number of samples are valid.
--- a/src/libprojectM/Audio/Loudness.cpp	2026-07-14 15:13:51
+++ b/src/libprojectM/Audio/Loudness.cpp	2026-09-21 19:44:07
@@ -1,6 +1,8 @@
 #include "Loudness.hpp"
 
 #include <cmath>
+#include <cstdlib>
+#include <algorithm>
 
 namespace libprojectM {
 namespace Audio {
@@ -30,6 +32,16 @@
 {
     int start = SpectrumSamples * static_cast<int>(m_band) / 6;
     int end = SpectrumSamples * (static_cast<int>(m_band) + 1) / 6;
+    // walkie: PM_BC_AUDIO=1 — butterchurn's bands, by frequency: 20-320 Hz, 320-2800 Hz,
+    // 2800-11025 Hz over a 1024-point FFT of 44.1 kHz samples (bin = 43.07 Hz).
+    static const bool bc = getenv("PM_BC_AUDIO") != nullptr;
+    if (bc)
+    {
+        static const float edges[4] = {20.0f, 320.0f, 2800.0f, 11025.0f};
+        auto bin = [](float hz) { int b = static_cast<int>(std::round(hz / (44100.0f / 1024.0f))) - 1; return std::max(0, std::min(SpectrumSamples - 1, b)); };
+        start = bin(edges[static_cast<int>(m_band)]);
+        end = bin(edges[static_cast<int>(m_band) + 1]);
+    }
 
     m_current = 0.0f;
     for (int sample = start; sample < end; sample++)
--- a/src/libprojectM/TimeKeeper.cpp	2026-07-14 15:13:51
+++ b/src/libprojectM/TimeKeeper.cpp	2026-09-21 19:46:28
@@ -1,6 +1,7 @@
 #include "TimeKeeper.hpp"
 
 #include <algorithm>
+#include <cstdlib>
 #include <random>
 
 namespace libprojectM {
@@ -19,6 +20,10 @@
     auto currentTime = std::chrono::high_resolution_clock::now();
 
     double currentFrameTime = std::chrono::duration<double>(currentTime - m_startTime).count();
+    // walkie: PM_TIME_SCALE=<k> runs the presets' clock at k times real time (the web
+    // route's `render({elapsedTime})`, i.e. HaloStyle.Preset.speed). Read every frame so
+    // a halo created later in the process with another speed is honoured.
+    { const char* e = getenv("PM_TIME_SCALE"); double k = e ? atof(e) : 1.0; if (k > 0.0 && k != 1.0) currentFrameTime *= k; }
     m_secondsSinceLastFrame = currentFrameTime - m_currentTime;
     m_currentTime = currentFrameTime;
     m_presetFrameA++;
--- a/src/libprojectM/ProjectM.cpp	2026-09-21 19:47:08
+++ b/src/libprojectM/ProjectM.cpp	2026-09-21 20:01:20
@@ -169,8 +169,8 @@
     // ToDo: Call the to-be-implemented render method in Renderer
     // walkie: PM_AUDIO_LOG=1 prints the per-frame audio statistics the presets read.
     { static const bool log = getenv("PM_AUDIO_LOG") != nullptr;
-      if (log) fprintf(stderr, "[pmaudio] {\"frame\": %u, \"bass\": %.3f, \"mid\": %.3f, \"treb\": %.3f, \"bass_att\": %.3f, \"mid_att\": %.3f, \"treb_att\": %.3f}\n",
-                       m_frameCount, audioData.bass, audioData.mid, audioData.treb, audioData.bassAtt, audioData.midAtt, audioData.trebAtt);
+      if (log) fprintf(stderr, "[pmaudio] {\"frame\": %u, \"time\": %.3f, \"bass\": %.3f, \"mid\": %.3f, \"treb\": %.3f, \"bass_att\": %.3f, \"mid_att\": %.3f, \"treb_att\": %.3f}\n",
+                       m_frameCount, m_timeKeeper->GetRunningTime(), audioData.bass, audioData.mid, audioData.treb, audioData.bassAtt, audioData.midAtt, audioData.trebAtt);
       if (log) { double ss = 0, sm = 0, ws = 0, wm = 0; for (auto v : audioData.spectrumLeft) { ss += v; if (v > sm) sm = v; }
                  for (auto v : audioData.waveformLeft) { ws += std::fabs(v); if (std::fabs(v) > wm) wm = std::fabs(v); }
                  fprintf(stderr, "[pmspec] {\"frame\": %u, \"spec_sum\": %.2f, \"spec_max\": %.3f, \"wave_abs_mean\": %.3f, \"wave_abs_max\": %.3f}\n",
